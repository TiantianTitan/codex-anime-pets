# Zhu Yuan look row 10 — two-guide final synthesis

Generate one complete coherent 8-pose row using the supplied first-four and last-four transition guides as the authoritative motion sequence. Use the supplied too-large and too-small full-row references only as a visual size bracket, and redraw at their exact midpoint. Do not crop, resize, paste, trace, or edit supplied pixels.

## Exact pose order

`180, 202.5, 225, 247.5, 270, 292.5, 315, 337.5`

## Motion lock

- Poses 1–4 must follow the approved first-four guide: face progresses `frontal/down → slight down-left → clear down-left → strong near-profile down-left`; ponytail/red tie progresses `image-left/rear → left-center behind crown → centered/partly occluded → image-right/rear` without any side teleport.
- Poses 5–8 must follow the approved last-four guide: `270` is unmistakably IMAGE-LEFT; then 292.5, 315, and 337.5 remain up-left and reopen smoothly toward approved 000 while the ponytail crosses behind the crown into the 000-compatible side.
- The transition from pose 4 to pose 5 must be one ordinary step within the same image-left family.
- Preserve canonical Zhu Yuan identity, face, orange-red front streak, high ponytail, uniform, armor, holster, sidearm, proportions, clean outlines, cel shading, and character-side asymmetry.

## Size lock

- Redraw all eight complete silhouettes at one uniform size exactly midway between the supplied `too-large` and `too-small` full-row references.
- Target roughly 90–92% of the too-large silhouette height and 110–113% of the too-small height.
- Every pose must visibly fall between the two reference sizes, maintain natural full-body proportions, and share one shoe baseline.
- Leave a clear uninterrupted green safety band above every ponytail and below every shoe; keep generous green gutters between poses and at both outer edges.

## Output contract

- Exactly 8 complete separated full-body sprites in one horizontal strip.
- Perfectly flat uniform `#00FF00` background only.
- No text, labels, degree marks, guides, borders, shadows, particles, detached effects, extra props, scenery, overlap, or clipping.

Reject if any face points image-right after 180; if 270 is ambiguous; if ponytail/red tie jumps sides; if 292.5–337.5 reverse; if scale matches either extreme rather than the midpoint; or if any figure differs in size/baseline.

All directions are literal viewer/screen coordinates. Draw the full row together as one new coherent image; never patch one cell.
