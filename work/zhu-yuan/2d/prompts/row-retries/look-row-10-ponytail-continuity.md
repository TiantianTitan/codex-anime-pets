Regenerate the complete eight-pose `look-row-10` strip for Codex pet `zhu-yuan`.

The rejected reference already gets `180 down`, `270 screen-left`, scale, baseline, and separation right. Preserve those properties. Repair the final upward-left quarter without changing direction meaning.

Output exactly eight complete separated full-body poses in one horizontal row on a perfectly flat pure green `#00FF00` background, ordered:

`180 down`, `202.5 down-left`, `225 down-left`, `247.5 down-left`, `270 screen-left`, `292.5 up-left`, `315 up-left`, `337.5 up-left`.

Screen directions are viewer/image coordinates. Slot 5 must remain an unmistakable screen-left profile: nose tip, pupils, and face surface point toward the image-left edge. Slots 6–8 must keep a leftward facial bias while the eyes and chin pitch upward; they must never become screen-right. Slot 8 is almost frontal but still slightly up-left, one ordinary step before the approved `000 up` pose.

Separate facial direction from ponytail follow-through. The face stays up-left in slots 6–8 while the high ponytail travels behind the skull toward its approved `000` placement. From slots 5→8, move the ponytail attachment and bulk gradually: image-right/rear at `270`, closer behind the head at `292.5`, crossing behind without detaching at `315`, then mostly on the same image-left side and at the same height as approved `000` by `337.5`. Do not flip the ponytail suddenly between `337.5` and `000`. The orange-red front hair streak is a separate canonical marking and must not swap sides.

Keep feet, pelvis, lower torso, head size, body height, and baseline registered to the approved row-9 strip. Preserve the canonical face, uniform, armor, holster, sidearm, palette, and proportions. All equipment stays attached and on its canonical body side. Use equal visual changes between neighboring poses, including `157.5→180` and `337.5→000`.

No whole-sprite rotation, mirroring, skew, scale changes, profile-to-front snap, overlapping poses, edge cropping, labels, text, guide marks, shadows, scenery, replacement eyes, detached effects, or detached hair/equipment.

Before accepting, count exactly eight pose groups and compare the last pose directly with approved `000`: the face must be one small left/up step away and the ponytail must already be on the compatible side, not waiting to flip on the loop boundary.
