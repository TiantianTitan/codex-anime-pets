# Zhu Yuan look row 10 — final 337.5 to 000 ponytail wrap

Regenerate the complete eight-pose row together. Preserve the supplied newest row's accepted identity, gaze directions, compact safe geometry, stable baseline, attached sidearm, and now-continuous `157.5 -> 180` boundary. Change only the late ponytail crossover so `337.5 -> 000` becomes one ordinary step.

## Non-negotiable final two poses

- At `315 up-left` (pose 7), the large ponytail must be passing **behind the skull**, mostly occluded and centered around the crown. It must no longer remain as a full mass on image-right.
- At `337.5 up-left` (pose 8), the ponytail bulk, outer silhouette, and red tie must be **clearly on the image-left side of the head**, already matching the approved `000 up` hairstyle placement. Only a small attached remnant may remain behind the image-right edge.
- Do not put the ponytail on image-right at pose 8. Do not treat the fixed orange-red front bang as the ponytail; the red tie and the entire large tied hair mass must visibly cross to image-left.
- Keep pose 8's face broad, near-frontal, and subtly up-left: high pupils, lifted chin, slight nose/facial-plane bias toward viewer-left. Hair crossover must not reverse the face direction.

## Preserve the rest

- Exactly eight complete separated poses: `180, 202.5, 225, 247.5, 270, 292.5, 315, 337.5`.
- `180` remains frontal/down with the ponytail on image-left like row-9 `157.5`; then the tail passes behind center and reaches image-right/rear by the unmistakable screen-left `270` profile.
- `292.5` remains up-left while the tail begins moving inward; `315` centers it behind the crown; `337.5` completes the crossover to image-left.
- All direction quadrants remain unchanged. Poses 2–4 down-left, pose 5 screen-left, poses 6–8 up-left; no screen-right reversal.
- Preserve Zhu Yuan's face, orange-red front streak, high ponytail/red tie, uniform, armor, holster, sidearm, proportions, palette, outlines, and cel shading.
- Preserve safe height/top clearance, broad green gutters, one shoe baseline, and stable head/body scale.

## Output contract

- One perfectly flat uniform `#00FF00` horizontal strip.
- Exactly eight foreground groups; no overlap, clipping, detached/split duplicate ponytail, shadows, particles, labels, guide marks, text, or scenery.

Reject if pose 8's large tied hair mass is still on image-right, if it jumps sides only at `000`, if any gaze direction changes, or if registered edges/scale/baseline regress. Draw the full row together; never patch, mirror, resize, or transform an individual cell.
